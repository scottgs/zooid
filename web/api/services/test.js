// Signal.create({}, console.log )

// var net = require('net');
// var server = net.createServer(function(socket) {
//   // do what you need
//    Signal.create({service:"finished"}, function(err, signal){
//       socket.write( new Buffer( signal.toJSON() ) );
//       socket.end("ENDED");
      
//    })
  
// });
// server.listen(8080);